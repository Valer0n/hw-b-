import "./Header.css";

export default function Header(props) {
  return (
    <>
      <header className="header">
        <h2>Burger Builder App</h2>
        {/* <button onClick={() => props.addOneElement({ name: "sth", price: 2 })}>
          {" "}
          Add
        </button> */}
      </header>
    </>
  );
}
